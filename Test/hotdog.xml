<scene version="2.0.0">
    <integrator type="path">
    </integrator>

    <!-- Instantiate a perspective camera with 45 degrees field of view -->
    <sensor type="perspective">
        <!-- Rotate the camera around the Y axis by 180 degrees -->
        <transform name="to_world">
            <lookat origin="2,5,2" target="0,0,0" up="0, 1, 0"/>
        </transform>
        <float name="fov" value="45"/>

        <!-- Render with 32 samples per pixel using a basic
             independent sampling strategy -->
        <sampler type="independent">
            <integer name="sample_count" value="32"/>
        </sampler>

        <!-- Generate an EXR image at HD resolution -->
        <film type="hdrfilm">
            <integer name="width" value="1920"/>
            <integer name="height" value="1080"/>
        </film>
    </sensor>

    <!-- Add a dragon mesh made of rough glass (stored as OBJ file) -->
    <shape type="obj">
        <string name="filename" value="hotdog.obj"/>

        <bsdf type="diffuse">
            <rgb name="reflectance" value="0.2, 0.25, 0.8"/>
        </bsdf>

    </shape>


    <emitter type="point">
        <spectrum name="intensity" value="100"/>
        <point name="position" x="-10" y="0" z="0"/>
    </emitter>

    <emitter type="point">
        <spectrum name="intensity" value="100"/>
        <point name="position" x="10" y="0" z="0"/>
    </emitter>

    <emitter type="point">
        <spectrum name="intensity" value="100"/>
        <point name="position" x="0" y="10" z="0"/>
    </emitter>

    <emitter type="point">
        <spectrum name="intensity" value="100"/>
        <point name="position" x="0" y="-10" z="0"/>
    </emitter>

    <emitter type="point">
        <spectrum name="intensity" value="100"/>
        <point name="position" x="0" y="0" z="10"/>
    </emitter>

    <emitter type="point">
        <spectrum name="intensity" value="100"/>
        <point name="position" x="0" y="0" z="-10"/>
    </emitter>



</scene>